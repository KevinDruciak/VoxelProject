#include "state_handler.h"

StateHandler::GameState StateHandler::interpolate(GameState const & current, GameState const & previous, float alpha) {
    GameState interpolated_state;
    
    return interpolated_state;
}

