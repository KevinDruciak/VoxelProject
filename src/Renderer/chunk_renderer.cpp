#include "chunk_renderer.h"

void ChunkRenderer::addChunk(const ChunkMesh& mesh) {
    //chunks.push_back(&mesh.getModel().getRenderInfo());
}

void ChunkRenderer::renderChunk(Camera& camera) {
    //return if no chunks to render
    //if (m_chunks.empty()) {
    //    return;
    //}

    //glDisable(GL_BLEND);
    //glEnable(GL_CULL_FACE);

    
    //use chunk shader
    //m_shader.useProgram();
    
    
    //BlockDatabase::get().textureAtlas.bindTexture();

    //m_shader.loadProjectionViewMatrix(camera.getProjectionViewMatrix());

    //for (auto mesh : m_chunks) {
    //    GL::bindVAO(mesh->vao);
    //   GL::drawElements(mesh->indicesCount);
    //}

    //m_chunks.clear();
}
